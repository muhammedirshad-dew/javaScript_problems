// print numbers in revers from 50 onwards

// initialize an empty array

let reversenum = [];

// iterate from 50 to 1

for (let i = 50; i >= 1; i--) {
    // add the current numbers to the array
    reversenum.push(i);
}
console.log(reversenum);